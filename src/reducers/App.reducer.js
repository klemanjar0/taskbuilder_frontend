const INITIAL_STATE = {

}

const appReducer = (currentState = INITIAL_STATE, {type, payload}) => {
    switch (type) {
        default:
            return currentState;
    }
}

export default appReducer;
